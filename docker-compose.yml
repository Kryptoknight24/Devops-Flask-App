version: '3.8'

services:
  # Container 1: The Web Application
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - mysql-db

  # Container 2: The Database
  mysql-db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: myappdb
    ports:
      - "3306:3306"